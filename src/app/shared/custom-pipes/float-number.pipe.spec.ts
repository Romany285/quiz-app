import { FloatNumberPipe } from './float-number.pipe';

describe('FloatNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new FloatNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
